Act as the billing-systems engineer picking up a regulated utility's mis-billing docket. The control-plane biller at `/app/workflow/rate_bill.py` turns meter reads into itemised bills from a rate table that fell behind the docket, and it evaluates stale draft rules rather than the commission's final decisions. Nothing the biller produces can be trusted until that rate table is rebuilt. The filed base tariff and the dated amendment filings survive alongside the stale `/app/data/effective_rate_table.json`. Which filings carry force, the order they consolidate in, how a later filing settles an earlier one, and what the result may carry are governance decisions rather than your choice; the table has to be consolidated in place at its expected path before the biller is worth running.

Then restore the biller itself. Preserve its `--input` and `--output-dir` command-line options and their defaults, and always read the consolidated rate table, the service-class register and the billing policy from their fixed absolute paths under `/app/data`; `--input` selects the meter-read file only. Consolidation is the separate step above and has already run by then: the biller bills from the table at that path and takes it as authoritative rather than consolidating the filed sources again for itself, so a table consolidated wrongly produces wrong bills. `/app/docs/report_spec.json` is the output contract: paths, schemas, required-field lists, field coercions, container shapes and sort orders. It says nothing about how any value is derived. Reconstruct that from `/app/incident/tariff_governance_log.md`, which is mostly routine noise and records rules drafted, revised and reversed over several months; where entries conflict, the later dated decision governs. A run writes exactly `/app/output/billing_summary.json`, `/app/output/bill_register.json` and `/app/output/exception_queue.jsonl`. Derive every value from the operational inputs, never from anything the grader holds: the standard library only, nothing under the contract's `workflow_repair.prohibited_imports` and nothing loaded at run time through `workflow_repair.prohibited_dynamic_loading`, correct against an alternate meter-read file, identical across reruns. The filed base tariff, the amendment filings, the service-class register, the billing policy and `/app/docs/report_spec.json` are read and not rewritten, so they all come back byte for byte unchanged, and the frozen snapshot `/app/workflow/.rate_bill.original` is left exactly as it is.
